<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    tools:context=".ui.home.HomeFragment">

    <TextView
        android:id="@+id/tv_welcome_user"
        style="@style/Poppins.Headline6"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/welcome_user"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Halo, Budi" />

    <TextView
        android:id="@+id/tv_welcome_message"
        style="@style/Poppins.Subtitle2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Selamat beraktifitas hari ini"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_welcome_user" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_home_menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:overScrollMode="never"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:layout_constraintTop_toBottomOf="@id/img_slider"
        app:spanCount="3"
        tools:itemCount="7"
        tools:listitem="@layout/item_menu_home" />

    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/loading_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:indeterminate="true"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/rv_home_menu"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/rv_home_menu" />

    <com.smarteist.autoimageslider.SliderView
        android:id="@+id/img_slider"
        android:layout_width="match_parent"
        android:layout_height="208dp"
        android:layout_marginTop="14dp"
        app:layout_constraintTop_toBottomOf="@id/tv_welcome_message"
        app:sliderAnimationDuration="1500"
        app:sliderAutoCycleDirection="right"
        app:sliderIndicatorAnimationDuration="1500"
        app:sliderIndicatorGravity="bottom|center_horizontal"
        app:sliderIndicatorMargin="6dp"
        app:sliderIndicatorMarginBottom="6dp"
        app:sliderIndicatorOrientation="horizontal"
        app:sliderIndicatorPadding="2dp"
        app:sliderIndicatorRadius="3dp"
        app:sliderIndicatorSelectedColor="@color/orange"
        app:sliderIndicatorUnselectedColor="@color/grey_soft"
        app:sliderScrollTimeInSec="5" />

</androidx.constraintlayout.widget.ConstraintLayout>